apiVersion: v1
kind: Service
metadata:
  name: frontend-checkout-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: tcc-frontend-checkout
  ports:
    - port: 80
      targetPort: 80
